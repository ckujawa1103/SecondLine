{
  "name": "secondline-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute secondline --remote --file=./schema.sql",
    "db:init:local": "wrangler d1 execute secondline --local --file=./schema.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.1.1"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
